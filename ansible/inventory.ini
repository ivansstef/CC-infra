[cinema]
# Цей файл використовується тільки для локального тестування
# Для CI/CD деплою inventory генерується динамічно з GitHub Secrets

# Для локального тестування розкоментуйте:
# localhost ansible_connection=local

# АБО додайте свій тестовий сервер:
# your-test-server ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa
