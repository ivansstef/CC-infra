# 🔄 Процес автоматичного деплою Cinema Club

## Архітектура

```
┌─────────────────┐
│   Developer     │
│   (Push code)   │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│              GitHub Repository                          │
│  ┌───────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  CC-infra     │  │  CinemaClub  │  │   Secrets    │ │
│  │  (Ansible)    │  │  (App code)  │  │   (Config)   │ │
│  └───────┬───────┘  └──────┬───────┘  └──────┬───────┘ │
└──────────┼──────────────────┼──────────────────┼─────────┘
           │                  │                  │
           │                  │                  │
           ▼                  ▼                  ▼
┌─────────────────────────────────────────────────────────┐
│              GitHub Actions (Runners)                   │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Workflow 1: Gitleaks (Security Scan)            │  │
│  │  ✓ Scan for secrets                              │  │
│  │  ✓ Run on: push, PR, daily                       │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Workflow 2: Deploy Infrastructure               │  │
│  │  1. Install Ansible                              │  │
│  │  2. Configure SSH (from Secrets)                 │  │
│  │  3. Create dynamic inventory                     │  │
│  │  4. Run: ansible-playbook deploy.yml             │  │
│  └────────────────────┬─────────────────────────────┘  │
│                       │                                 │
│  ┌────────────────────▼─────────────────────────────┐  │
│  │  Workflow 3: Deploy Application                  │  │
│  │  1. Checkout CinemaClub repo                     │  │
│  │  2. Build Docker image                           │  │
│  │  3. Push to Docker Hub                           │  │
│  │  4. SSH to server & deploy                       │  │
│  └────────────────────┬─────────────────────────────┘  │
└───────────────────────┼─────────────────────────────────┘
                        │
                        │ SSH Connection
                        ▼
┌─────────────────────────────────────────────────────────┐
│              Target Server (Ubuntu 22.04)               │
│                                                          │
│  Ansible роль "cinema" встановлює:                      │
│  ┌────────────────────────────────────────────────┐    │
│  │  System Packages                               │    │
│  │  • git, python3, pip                           │    │
│  │  • docker.io, docker-compose                   │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  /opt/cinema-club/                             │    │
│  │  • Clone CinemaClub repo                       │    │
│  │  • docker-compose.yml                          │    │
│  │  • Docker containers                           │    │
│  └─────────────────┬──────────────────────────────┘    │
│                    │                                     │
│                    ▼                                     │
│  ┌────────────────────────────────────────────────┐    │
│  │  Docker Container: cinema-club                 │    │
│  │  • Flask application                           │    │
│  │  • Port: 8080                                  │    │
│  │  • Network: cinema-network                     │    │
│  └────────────────────────────────────────────────┘    │
└──────────────────────────┬──────────────────────────────┘
                           │
                           │ HTTP :8080
                           ▼
                    ┌─────────────┐
                    │   Browser   │
                    │   (Users)   │
                    └─────────────┘
```

## 🔄 Потоки деплою

### 1. Інфраструктура (Infrastructure Deployment)

```
Developer → Push to main → GitHub Actions → Ansible Playbook → Server Setup
```

**Кроки:**
1. Розробник робить зміни в `ansible/**`
2. Push в `main` branch
3. GitHub Actions запускає `deploy_infra.yml`
4. Ansible підключається до сервера
5. Встановлює пакети, клонує код, запускає Docker

**Тривалість:** ~5-10 хвилин

### 2. Додаток (Application Deployment)

```
Developer → Manual trigger → Build Docker → Push to Hub → Deploy to Server
```

**Кроки:**
1. Ручний запуск workflow або webhook
2. GitHub Actions збирає Docker image з CinemaClub
3. Image публікується на Docker Hub
4. SSH на сервер → `docker-compose pull && up -d`
5. Перевірка доступності

**Тривалість:** ~3-5 хвилин

### 3. Безпека (Security Scan)

```
Any push/PR → Gitleaks scan → Report results
```

**Кроки:**
1. Будь-який push або PR
2. Gitleaks сканує код на секрети
3. Результат в Actions tab
4. Блокування merge якщо знайдено секрети

**Тривалість:** ~30 секунд

## 🔐 Секрети та конфігурація

### Де зберігаються дані:

| Тип даних | Місце зберігання | Використання |
|-----------|------------------|--------------|
| SSH ключі | GitHub Secrets | Підключення до сервера |
| Server IP | GitHub Secrets | Динамічний inventory |
| Docker credentials | GitHub Secrets | Push images |
| Ansible код | Git repository | Версіоноване |
| Конфігурація додатку | Docker Compose template | Параметризоване |

### Чому це безпечно:

✅ Секрети зашифровані в GitHub  
✅ Не відображаються в логах  
✅ Доступні тільки в runtime  
✅ Можна оновити без зміни коду  
✅ Різні secrets для різних environments  

## 📊 Моніторинг деплою

### Де дивитись логи:

1. **GitHub Actions:**
   - Repository → Actions tab
   - Кожен workflow має детальні логи

2. **На сервері:**
   ```bash
   # Docker логи
   docker-compose logs -f
   
   # System logs
   journalctl -u docker -f
   ```

3. **Application logs:**
   ```bash
   cd /opt/cinema-club
   docker-compose logs cinema-club
   ```

## 🚨 Rollback стратегія

### Відкат інфраструктури:
```bash
# Локально через Ansible
ansible-playbook -i ansible/inventory.ini ansible/deploy.yml --tags=deploy
```

### Відкат додатку:
```bash
# На сервері
cd /opt/cinema-club
docker-compose down
docker pull yourusername/cinema-club:previous-sha
docker-compose up -d
```

Або через GitHub Actions - перезапустити старий успішний workflow.

## 🎯 Best Practices

1. **Завжди тестуйте спочатку на staging**
2. **Використовуйте версійні теги Docker images**
3. **Зберігайте backup inventory.ini локально**
4. **Регулярно оновлюйте SSH ключі**
5. **Моніторте логи після кожного деплою**

---

**Результат:** Повністю автоматизований, безпечний та відслідковуваний процес деплою! 🚀
